The notebook neatly covers five fundamental NumPy areas:

Array Exploration: Inspecting ndim, shape, and size.

Slicing: Isolating columns (like steps) or subsets of rows.

Reshaping: Changing array dimensions with .reshape().

Boolean Filtering: Extracting rows based on conditions (e.g., steps > 8000).

Statistical Functions: Computing np.mean(), np.max(), np.min(), and np.std().